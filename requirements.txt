# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.1
python-multipart>=0.0.9
pyyaml>=6.0.2
pydantic>=2.9.0
pydantic-settings>=2.6.0

# HTTP (JSON-2 Odoo API + scraping)
httpx>=0.27.0
feedparser>=6.0.11
beautifulsoup4>=4.12.3
lxml>=5.3.0

# Scheduling
apscheduler>=3.10.4

# Utils
python-dateutil>=2.9.0
jinja2>=3.1.4
aiofiles>=24.1.0
rich>=13.9.0

# MCP — tools for AI agents (Claude Desktop / Claude Code / etc.)
mcp>=1.0.0

# ===== Memory & Backup =====
# Memory: local multilingual embeddings (optional, MEMORY_EMBEDDINGS=auto)
fastembed>=0.4.0
numpy>=1.26.0
# Backup: Google Drive auth
PyJWT>=2.9.0
pywebpush>=1.14.0  # Web Push notifications
python-docx>=1.1.0  # Word (.docx) report export
cryptography>=43.0.0

# Optional: JS page rendering for interactive official portals (run once: python -m playwright install chromium)
playwright>=1.40.0
